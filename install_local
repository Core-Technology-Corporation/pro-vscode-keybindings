#!/bin/bash
set -e
cd "$(dirname "$0")"

echo "==> Installing dependencies"
npm install

echo "==> Building (keybindings + extension bundle)"
npm run build

echo "==> Launching Extension Development Host"
code --extensionDevelopmentPath="$(pwd)" --new-window
